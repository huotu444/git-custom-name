/clear
1.你直接修改代码
2.有任何疑问直接问我
我正在使用 STM32F103C8T6 和 HAL 库开发智能小车。目前硬件测试已全部通过，接下来需要完成小车的核心功能模块封装：基础运动控制、蓝牙串口通信、超声波避障。
请帮我生成结构清晰的模块化代码。

这是我硬件的实际引脚连接情况，请严格按照此配置生成代码：
【电机驱动 (TB6612，STBY已硬接5V)】
- 左轮：PWMA -> PA8 (TIM1_CH1), AIN1 -> PB15, AIN2 -> PB14
- 右轮：PWMB -> PA11 (TIM1_CH4), BIN1 -> PB13, BIN2 -> PB12

【蓝牙模块 (USART1)】
- STM32 RX -> PA10，STM32 TX -> PA9

【超声波模块 (HC-SR04)】
- Trig -> PA4 (普通 GPIO 输出)
- Echo -> PB7 (请告诉我如何配置，是普通输入还是定时器输入捕获？)

我的具体需求如下，请分步骤详细解答：

1. 【CubeMX 配置清单】：
   - 除了电机 PWM 定时器，还需要开启哪些外设？(例如：蓝牙的 USART1 中断、超声波需要的微秒级延时定时器等)。请列出清晰的配置参数。

2. 【生成 motor.c 和 motor.h】：
   - 封装基础运动函数：`Car_Forward()`, `Car_Backward()`, `Car_TurnLeft()`, `Car_TurnRight()`, `Car_Stop()`。速度可以通过参数传入。

3. 【生成 bluetooth.c 和 bluetooth.h】：
   - 使用中断方式接收蓝牙数据。
   - 编写一个简单的解析逻辑：接收到字符 'F' 前进，'B' 后退，'L' 左转，'R' 右转，'S' 停止。

4. 【生成 ultrasonic.c 和 ultrasonic.h】：
   - 封装测距函数 `float Ultrasonic_GetDistance(void)`，返回厘米(cm)值。

5. 【综合主逻辑 main.c】：
   - 在 `while(1)` 中写一个基础逻辑：平时根据超声波距离自动避障（比如距离小于 15cm 就后退并左转），但如果接收到蓝牙指令，则优先执行蓝牙指令。
   /clear
循迹：
/clear
我正在使用 STM32F103C8T6 和 HAL 库开发智能小车。目前需要添加“串口八路灰度循迹模块”的驱动，并使用 PD 控制算法实现平滑巡线。

【硬件连接与环境】
- STM32 RX -> PA3，STM32 TX -> PA2 (对应 USART2)。
- 循迹场地：白底黑线。

【🚨 模块专属通信协议（非常重要）】
1. 波特率必须是 115200。
2. 模块上电默认不发送数据。STM32 必须先通过 USART2 发送字符串指令 "$0,0,1#" 告诉模块开启“数字型数据”发送。
3. 模块回传的数据格式是带有帧头 '$' 和帧尾 '#' 的字符串。格式为："$D,x1:v,x2:v,x3:v,x4:v,x5:v,x6:v,x7:v,x8:v#"。（其中的 v 是 0 或 1，1代表压在黑线上）。

我的需求如下，请分步骤提供结构清晰的代码：

1. 【CubeMX 配置】：
   - 提醒我配置 USART2 (115200波特率)，并开启全局中断。

2. 【生成 track.c 和 track.h】：
   - 编写 `Track_Init(void)` 函数，在里面通过 HAL_UART_Transmit 发送开启指令 "$0,0,1#"。
   - 编写串口接收处理逻辑。考虑到是以 '#' 结尾的变长字符串，请实现一个可靠的接收方式（比如每次接收1个字符，遇到 '#' 后进行解析）。
   - 编写字符串解析逻辑，提取出 x1 到 x8 的值，存入一个全局数组 `uint8_t Sensor_Data[8]` 中。
   - 编写核心逻辑 `float Track_GetError(void)`。对 `Sensor_Data` 数组进行加权计算，得出偏离中心线的误差值（完全居中返回 0，偏左返回负数，偏右返回正数）。必须加入“丢失黑线保护逻辑”：如果读到全白，保持返回上一次的极限误差值。

3. 【生成 pid.c 和 pid.h】：
   - 实现一个标准的位置式 PD 控制器结构体及初始化函数。
   - 编写 `float PID_Calculate(PID_TypeDef *pid, float target, float current)` 函数。

4. 【巡线主逻辑】：
   - 编写 `Track_FollowLine()`。设定 target 为 0，将 `Track_GetError()` 的结果作为 current 传入 PID，得出转向补偿值 `turn_out`。
   - 结合基础速度，计算出左右轮最终速度，并调用底层的 `Car_SetSpeed(left, right)` 驱动小车。

   /clear
我正在基于 STM32F103C8T6 开发电赛 H 题。现在需要实现一个 OLED 任务选择菜单。

【硬件配置】
- OLED (0.96寸 I2C)：SCL -> PB8, SDA -> PB9。
- 交互按键：Mode1 -> PC14, Mode2 -> PC15 (按下为低电平)。
- 反馈系统：LED -> PA12, 蜂鸣器 -> PA15。

【核心需求】
1. 实现 oled_menu.c 和 button.c 模块。
2. 菜单显示：在 OLED 上列出 "Task 1", "Task 2", "Task 3", "Task 4"。
3. 按键交互逻辑：
   - 短按 PC14 或 PC15 分别实现菜单光标的循环切换（上/下）。
   - 双击任一按键：确认当前选中的 Task。
   - 确认反馈：蜂鸣器响 100ms，LED 闪烁一下。
4. 特别注意：
   - PA15 默认是 JTAG 引脚，请务必在初始化代码中包含禁用 JTAG (但保留 SWD) 的 remap 命令。
   - 编写一个非阻塞的按键扫描函数 Button_Scan()，支持双击判定逻辑。
   - 预留 Task_Start(uint8_t task_num) 函数接口，以便我后续填入不同任务的控制逻辑。

请生成结构化的 HAL 库代码，并注释掉之前代码中与蓝牙相关的初始化和中断逻辑。